<div>
	{{burger_name}}
	<button class="changeButton" data-id="{{id}}" data-newdevoured="{{devoured}}">
		{{#if devoured}}ORDER AGAIN!{{else}}DEVOUR!{{/if}}
		{{!-- {{#unless devoured}}DEVOUR IT!{{/unless}} --}}
	</button>
</div>